services:
  rupheus-database:
    image: "postgres:latest"
    container_name: "rupheus-database"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  rupheus-server:
    build:
      context: "./application"
      dockerfile: "Dockerfile"
    image: "rupheus:latest"
    container_name: "rupheus-server"
    ports:
      - "8080:8080"
    depends_on:
      rupheus-database:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  data:
